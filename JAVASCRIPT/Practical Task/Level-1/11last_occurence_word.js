function last_occurence(str) {
    return str.lastIndexOf("because");
}

console.log("Task 11");
console.log(last_occurence("You cannot end a sentence with because because because is a conjuction."));

